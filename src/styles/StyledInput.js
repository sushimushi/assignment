import styled from "styled-components";

const StyledListItem = styled.input`
    background-color: var(--dark-grey);
    border: none;
    width:100%;
    margin-bottom: 1rem;
    color: var(--white);
    font-size: var(--fs-md);
    padding:1rem;
    border-radius: var(--border-radius-subtle);
`;

export default StyledListItem;